# Secrets — NEVER commit
.env

# Python caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.deepeval/

# Backups
*.bak

# Full JMdict (117MB) is fetch-on-demand (scripts/fetch_data.py); the common slice + the derived
# ja catalogs (name surfaces / collocations / pitch accent) are BUNDLED so a fresh clone does
# Japanese with zero fetches — they have no in-repo builder, losing them = losing ja.
data/jmdict-eng-3.6.2.json
*.thoth.txt

# multilang build artifacts (large/raw/scratch)
data/leipzig/
data/samples/
_bench.py
_bench2.py
_build_all.py

# session scratch / automation one-offs
*.mjs
_*.py
*.log
setup_models.log
bench/battery/*.wav
.coverage
